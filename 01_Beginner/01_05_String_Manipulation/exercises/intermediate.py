# Write a function to count the number of vowels in a string.
# Replace all occurrences of "old" with "new" in a given string.
# Remove all whitespace from both sides and inside a string.
# Check whether a given string is a palindrome (ignore case and spaces).
# Format a string to display name, age, and city using f-string.