# Write a function that uses traceback.print_exc() when an error occurs.
# Create a debugging wrapper function that prints function name, arguments, and return value.
# Debug a nested function call and identify where the error actually happens.
# Write a program that intentionally raises ValueError and catches it with meaningful message.
# Implement a simple calculator that uses try-except for all operations and logs errors.
